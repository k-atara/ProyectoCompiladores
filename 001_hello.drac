binary(num) {
    var result, remainder;
    if (num <= 0) {
        return "0";
    }
    result = "";
    while (num > 0) {
        remainder = num % 2;
        add(result, remainder + '0');
        num = num / 2;
    }
    reverse(result);
    return result;
}